export const add = (arr) => { 
    return arr.reduce((total, number) => total + number); 
};

export const multiply = (arr) => { 
    return arr.reduce((total, number) => total *= number); 
};

export const subtract = (firstNum, secondNum) => firstNum - secondNum

export const division = (firstNum, secondNum) => firstNum / secondNum


